# InterviewMaster Phase 0 & Phase 1 开发排期与任务拆解

> 状态：历史排期草稿。其 OceanBase 和多微服务任务不再作为当前排期依据；后续将按 [`技术方案_v1.md`](技术方案_v1.md) 重新生成。

## 概述

本文档基于 InterviewMaster 的全流程开发大纲，专门针对 **Phase 0（项目基建与技术选型落地）** 和 **Phase 1（微服务拆分与接口契约）** 进行了详细的任务拆解、耗时预估及依赖管理。
整个阶段预计总耗时约 **4 - 7 个工作日**，旨在完成项目的从零启动、基建搭建、微服务骨架生成以及核心流程的最小化闭环（MVP 骨架）。

---

## Phase 0：项目基建与技术选型落地 (预计耗时: 1-3 天)

**阶段目标**：搭建统一的 Monorepo 仓库结构，完成核心基础设施的公共组件封装（日志、配置、数据库连接、统一响应等），确保后续微服务开发具备规范统一的底座。

### 0.1 仓库初始化与工程规范 (0.5 - 1 天)

- **[Task 0.1.1] 初始化 Monorepo 仓库**
  - **详情**：初始化 Git 仓库，建立标准的目录结构：`apps/` (微服务), `api/` (网关 API), `rpc/` (gRPC Proto), `pkg/` (公共基础组件), `deploy/` (部署配置)。
  - **产出**：清晰的目录骨架，基础的 `.gitignore`, `go.mod` 初始化。
- **[Task 0.1.2] go-zero 环境与自动化脚本**
  - **详情**：编写基础的 `Makefile` 工具链，提供统一命令（如 `make gen-api` / `make gen-rpc`）用于根据定义文件自动生成脚手架代码。
  - **产出**：可一键生成 API/RPC 代码的 Makefile。

### 0.2 公共组件库 (pkg) 开发 (1 - 1.5 天)

- **[Task 0.2.1] OceanBase/DB Client 封装**
  - **详情**：基于 GORM 或官方驱动封装数据库连接，支持多租户上下文传递、连接池配置及统一日志打印。
  - **产出**：`pkg/db` 组件包。
- **[Task 0.2.2] 鉴权中间件与 JWT 组件**
  - **详情**：实现 JWT token 的生成与解析逻辑，支持 HTTP (网关层) 和 RPC (内部服务) 的拦截器鉴权。
  - **产出**：`pkg/auth` 组件包。
- **[Task 0.2.3] 可观测性组件封装**
  - **详情**：集成结构化日志库（如 zap），实现 TraceId 在 HTTP/RPC/Asynq 全链路的注入与透传；接入基础 Prometheus Metrics。
  - **产出**：`pkg/logger` 和 `pkg/trace` 组件包。
- **[Task 0.2.4] API 统一响应与错误码规范**
  - **详情**：定义系统全局标准错误码（含业务错误与系统错误区分），以及规范 API 层的统一 JSON 返回格式。
  - **产出**：`pkg/errors` 和统一 Response 封装。

### 0.3 本地基础设施部署 (0.5 天)

- **[Task 0.3.1] 本地环境 docker-compose 搭建**
  - **详情**：编写 `docker-compose.yml`，一键启动本地依赖服务：OceanBase (或临时用 MySQL 平替)、Redis (缓存与队列依赖)。
  - **产出**：可通过 `docker-compose up -d` 启动的本地全套依赖环境。

---

## Phase 1：微服务拆分与接口契约落地 (预计耗时: 3-7 天)

**阶段目标**：基于 go-zero 完成核心微服务的拆分，定义各服务的 API 与 RPC 契约，并完成“前端发请求 -> 网关路由 -> RPC 处理 -> 异步任务投递与消费”的基础链路打通。

### 1.1 API 网关 (`gateway-api`) 搭建 (0.5 - 1 天)

- **[Task 1.1.1] 网关契约 (`.api`) 定义**
  - **详情**：编写 `gateway.api` 文件，定义前端所需的 RESTful API 路由（登录注册、简历上传、复盘触发等）。
  - **产出**：`api/gateway/gateway.api` 契约文档及生成的 Go HTTP 服务骨架。
- **[Task 1.1.2] 网关中间件集成**
  - **详情**：在 HTTP 层应用跨域 (CORS) 配置、请求限流机制以及 `pkg/auth` 权限拦截中间件。

### 1.2 用户服务 (`user-svc`) MVP (0.5 - 1 天)

- **[Task 1.2.1] 用户 `.proto` 契约与骨架生成**
  - **详情**：定义登录、注册、鉴权拉取等基础 RPC 接口，使用 go-zero 生成 RPC 服务。
  - **产出**：`rpc/user/user.proto` 及其对应的服务端桩代码。
- **[Task 1.2.2] 基础鉴权业务实现**
  - **详情**：实现用户的简单注册与登录逻辑，交互打通数据库 `im_user` 表，验证 JWT 下发流程。

### 1.3 简历服务 (`resume-svc`) 契约与上传流程 (1 - 2 天)

- **[Task 1.3.1] 简历 `.proto` 契约定义**
  - **详情**：定义简历上传、状态查询、触发解析等接口。
  - **产出**：`rpc/resume/resume.proto`。
- **[Task 1.3.2] 简历上传与元数据落库**
  - **详情**：打通网关上传文件入口，实现文件临时保存/对象存储对接，并在数据库生成 `im_resume` 初始记录（状态为 pending）。
- **[Task 1.3.3] 异步解析任务投递**
  - **详情**：集成 Asynq Client，在简历上传完成后投递解析任务到队列，返回 task_id 给调用方。

### 1.4 AI 调度骨架 (`rag-svc` & `interview-svc`) (1 天)

- **[Task 1.4.1] AI 核心调度 `.proto` 定义**
  - **详情**：定义 `Predict` (押题) 和 `Critique` (复盘批判) 等基于 RAG 的 RPC 契约。
  - **产出**：`rpc/rag/rag.proto` 和 `rpc/interview/interview.proto`。
- **[Task 1.4.2] 桩代码与 Mock 返回**
  - **详情**：由于 AI 逻辑在后续阶段实现，此处仅编写桩代码，打通 RPC 调用，验证参数传递，返回固定 Mock 结构化数据供联调。

### 1.5 异步任务引擎 (`ingestion-worker`) 集成 (1 天)

- **[Task 1.5.1] Asynq 消费者服务初始化**
  - **详情**：在 `apps/ingestion` 启动独立的 Asynq 消费端服务，注册多优先级队列（如 `q.realtime`, `q.batch`, `q.heavy`）。
- **[Task 1.5.2] 简历解析 Mock Handler 注册**
  - **详情**：编写 worker 处理逻辑，消费来自 `resume-svc` 投递的简历解析任务，模拟处理延时后更新数据库状态为 `completed`。

### 1.6 全链路联调与验收 (0.5 - 1 天)

- **[Task 1.6.1] “上传 -> 处理 -> 查询” 闭环测试**
  - **详情**：模拟真实请求路径：通过网关发起带 JWT 的请求 -> 触发简历上传 RPC -> 投递任务到 Asynq -> Worker 消费结束并改库 -> 网关查询状态完毕。
- **[Task 1.6.2] 可观测性验收**
  - **详情**：验证一次请求的 TraceId 是否正确从 HTTP Header 贯穿至 RPC 并在 Asynq 任务日志中一致体现，保证系统的可排查性。

---

## 阶段执行时间轴参考 (Gantt Chart 预估)

- **Day 1**: 启动阶段。完成 0.1 仓库初始化目录划分；搭建 0.3 基础环境 Docker Compose。
- **Day 2**: 组件封装。集中开发 0.2 的公共组件（DB、Auth、Logger、Trace、统一响应），并接入脚手架验证。
- **Day 3**: 契约前置。编写所有 `.api` 及 `.proto` 契约文件 (1.1.1, 1.2.1, 1.3.1, 1.4.1) 并通过工具生成全套服务骨架。
- **Day 4**: 网关与用户态。实现网关跨域与鉴权 (1.1.2) 和 User Service 核心鉴权业务闭环 (1.2.2)。
- **Day 5**: 简历流转。实现 Resume Service 上传落库逻辑 (1.3.2) 及异步任务投递逻辑 (1.3.3)。
- **Day 6**: 异步引擎。完成 Ingestion Worker 搭建并打通与简历服务的异步状态流转闭环 (1.5)。
- **Day 7**: 联调验收。实现 RAG 与 Interview 服务的 Mock 返回 (1.4.2)，完成全链路联调测试、Trace 验证及代码审查 (1.6)。
