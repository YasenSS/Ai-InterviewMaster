# InterviewMaster Agent 落地 TODO

> 生成日期：2026-08-12
> 依据：`docs/上线TODO.md`、`docs/agent设计.md` 与当前代码现状复核
> 目标：把现有规则版 Demo 升级为可灰度、可审计、可控成本、可回归验证的真实 AI 面试系统

---

## 0. 范围与完成标准

本清单只覆盖智能层及其直接依赖：模型 Provider、Prompt、简历理解、出题、动态追问、评分、报告、审计、成本、评测和相关前后端状态。

不在本轮实现：

- 自主多 Agent 协作。
- 通用长期记忆或上下文压缩。
- MVP 私有简历 RAG。
- MCP 工具生态。
- 大规模公共面经向量库。

首发架构保持：

```text
确定性工作流（简历抽取 → 蓝图 → 出题 → 评分 → 报告）
                    +
一处收窄的动态追问决策（由业务状态机控制出口）
```

### 状态标记

- `[ ]` 未开始。
- `[-]` 已有骨架或进行中，尚未通过验收。
- `[x]` 已完成并通过对应验收。
- `[!]` 被外部条件或产品决策阻塞。

### Agent MVP 完成定义

同时满足以下条件才可认为智能层具备邀请制上线能力：

1. 题目会因简历、JD 和目标岗位不同而产生可解释差异，不再返回固定五题。
2. 报告基于题目、回答和材料证据评分，不再按回答字数计算。
3. 所有模型输出先进入强类型结构并校验，非法输出不会直接写入业务表。
4. 每次调用可追溯到 Provider、模型、Prompt 版本、Token、耗时、费用和 Trace。
5. 模型超时、限流、不可用或输出非法时，用户能看到明确状态，并可安全重试或得到受控降级。
6. 每个用户有并发、调用次数和成本上限，不会出现无界调用。
7. Golden Dataset 和真实全流程 E2E 达到发布基线。

---

## 1. 已确认的设计决策

以下决策作为本轮实现约束，不在编码过程中反复摇摆：

| 问题 | 决策 |
| --- | --- |
| 架构形态 | 不做自主多 Agent；采用确定性工作流，动态追问保持有界 |
| 编排框架 | Eino 可用于 `platform/ai` 内部，领域 logic 不直接 import Eino |
| 业务依赖 | 领域层只依赖自研 `ChatModel`、`EmbeddingModel`、`Tool` 等接口 |
| MVP RAG | 不做；简历/JD 经长度控制后全文直喂，证据依靠事实 ID 与原文定位 |
| Embedding | MVP 不依赖；移除或停止新增 SHA-256 假向量，不把它作为真实能力展示 |
| 状态与记忆 | PostgreSQL 是权威状态；调用时按规则组装最近上下文，不使用独立 Agent Memory |
| 租户标识 | 当前产品以 `user_id` 隔离；在正式引入 workspace 表以前不混用虚构 `workspace_id` |
| 评分原则 | 模型输出各维证据与判断，Go 代码按固定规则计算总分 |
| 模型调用位置 | 长任务走 Worker；只有需要交互时延的追问允许同步调用 |
| 失败原则 | 失败显式可见；只对明确可重试错误重试，不伪造 AI 结果 |

---

## 2. 当前基线

### 已有基础

- [x] API、Worker、PostgreSQL、Redis、对象存储与异步任务骨架。
- [x] 简历上传、Tika 文本提取、JD、题集、面试状态机和报告数据表基础。
- [x] JWT + Refresh Session、用户级资源隔离、登录限流。
- [x] OpenTelemetry Trace 骨架与结构化 HTTP 日志。
- [-] `ChatModel`、`EmbeddingModel`、`Tool` 自研接口已经建立，但尚未注入业务。
- [-] `backend/prompts/` 目录及版本约定已经建立，但没有可执行 Prompt 和 Schema。

### 必须替换的 Stub

- [-] 启用 AI 时题集已走 Eino Graph；固定五题仅作为关闭 AI 时的本地开发回退，尚未增加 degraded 标记。
- [ ] `getinterviewreportlogic.go` 的字数评分和固定点评/参考答案。
- [ ] `resumeparse.go` 只提取首行作为事实的逻辑。
- [ ] `resumeparse.go` 的 `baseline-hash-1536` 假向量。
- [ ] `searchcompanyintellogic.go` 的固定面经内容；若 Beta 暂不上线，则隐藏入口并明确禁用。

---

## 3. P0-A：模型基础设施与配置

### AGENT-001 固化自研接口

- [x] 保留 `ChatModel.Generate`，补齐请求级 deadline、结构化输出约束和调用元数据。
- [x] 首发无流式 UI，暂不扩张 `Stream` 接口。
- [x] 将租户字段统一为当前真实存在的 `UserID`；未来引入工作区后再兼容 `WorkspaceID`。
- [-] 定义统一错误类型：已覆盖超时、限流、鉴权、供应商错误和输出非法；上下文超限、预算耗尽随治理层补充。
- [x] Provider 原始错误留在错误 cause 中，不直接形成客户端文案。
- [x] 为接口提供 Fake Model，Eino Graph 业务测试不依赖真实外部模型。

验收：

- 业务包不 import Eino 或供应商 SDK。
- Provider 可以被 Fake 实现替换，核心 workflow 单测无需网络。

### AGENT-002 接入一个真实 Provider

- [x] 固定 Eino `v0.9.13` 与 OpenAI 组件 `v0.1.13`，不使用 `@latest`。
- [x] 在 `backend/internal/platform/ai/provider/` 实现 OpenAI 兼容 ChatModel 适配。
- [x] 支持 Base URL、API Key、模型名、请求超时、最大输出 Token 等环境配置。
- [-] API `ServiceContext` 已注入模型实例；Worker 在首个 AI 异步任务接入时补充共享上下文。
- [x] 启动时校验启用状态下的 Provider、密钥、模型、超时和 Token 上限。
- [x] 为 Provider 实现连接错误、429、5xx 和超时的错误映射。

建议环境变量：

```text
IM_AI_PROVIDER=
IM_AI_BASE_URL=
IM_AI_API_KEY=
IM_AI_CHAT_MODEL=
IM_AI_SMALL_MODEL=
IM_AI_REQUEST_TIMEOUT=
IM_AI_MAX_OUTPUT_TOKENS=
```

验收：

- 本地可通过最小请求获得真实模型响应。
- 未配置密钥时开发环境可明确禁用 AI，生产环境启动失败。
- API Key 不进入日志、数据库输入快照或客户端响应。

### AGENT-003 调用可靠性策略

- [-] 已设置 HTTP Client 和整次调用 deadline；首字节超时等待流式调用阶段补充。
- [ ] 仅对网络瞬断、429 和可恢复 5xx 做指数退避，最多重试 2 次并加入 jitter。
- [ ] 4xx 参数错误、输出校验错误和预算拒绝不做传输重试。
- [ ] JSON 修复最多 1 次，且与传输重试分开计数。
- [ ] 每次重试都复用业务幂等键并单独留下 attempt 记录。
- [ ] Provider 不可用时支持受控降级；降级模型必须兼容同一输出 Schema。

---

## 4. P0-B：审计、成本与额度先行

### AGENT-010 新增 `model_invocations`

- [ ] 新建数据库迁移和查询，至少记录：

```text
id
user_id
task_id / session_id / resource_type / resource_id
provider / model
prompt_key / prompt_version
status / attempt
input_hash / output_hash
prompt_tokens / completion_tokens / total_tokens
estimated_cost
latency_ms
error_code
trace_id / request_id
created_at / completed_at
```

- [ ] 默认不保存完整简历、JD、回答和模型输出正文；调试采样需显式配置、脱敏且设置保留期。
- [ ] 为 `user_id + created_at`、`resource_id`、`trace_id` 建查询索引。
- [ ] 使用 Callback/统一包装器埋点，禁止各业务节点各自拼审计逻辑。
- [ ] 审计写入失败不能悄悄失联：记录基础设施错误，并根据环境选择阻断或降级。

### AGENT-011 成本和配额

- [ ] 设置单次最大输入字符/Token、最大输出 Token 和最大耗时。
- [ ] 设置用户级同时执行中的模型调用上限。
- [ ] 设置用户日调用次数与日/月预算软、硬阈值。
- [ ] 软阈值触发小模型或减少可选生成；硬阈值返回稳定的业务错误。
- [ ] 对题集、报告等资源使用版本/输入哈希避免重复生成。
- [ ] 明确管理员查看成本与异常用户的方式，首版可先提供 SQL/监控面板。

验收：

- 任意一次业务生成都能追踪 Token、耗时、成本和结果状态。
- 并发压测不能绕过用户硬额度。
- 重复请求不会无界重复计费或写入重复业务结果。

---

## 5. P0-C：Prompt 与强类型输出

### AGENT-020 建立可执行 Prompt 包

- [-] `question.generate/v1` 已建立并嵌入二进制；其余 Prompt 待后续节点接入：

```text
backend/prompts/resume.extract/v1/
backend/prompts/blueprint.generate/v1/
backend/prompts/question.generate/v1/
backend/prompts/evaluation.critique/v1/
backend/prompts/report.compose/v1/
```

- [ ] 每个目录包含 `system.md`、`task.md`、`meta.yaml` 和输出 Schema。
- [x] Prompt 加载器使用 `embed.FS`，容器运行时不依赖工作目录。
- [ ] Prompt 版本不可原地修改；升级时新增版本并保留回滚能力。
- [x] `question.generate/v1` 已将 System、任务、Schema 与不可信数据区分离；其余 Prompt 按同一规则实现。
- [ ] 定义输入裁剪顺序，禁止简单从末尾截断导致关键信息或分隔边界损坏。

### AGENT-021 定义领域输出契约

- [ ] 定义 `ResumeExtraction`：事实类别、事实值、置信度、`source_excerpt`、原文定位。
- [ ] 定义 `InterviewBlueprint`：目标能力、权重、难度、主问题数、追问预算、时间预算。
- [ ] 定义 `GeneratedQuestionSet`：题目、意图、期望点、能力键、难度、证据 fact IDs、追问建议。
- [ ] 定义 `TurnEvaluation`：各评分维度、评分理由、证据、缺失信息和改进建议。
- [ ] 定义 `InterviewReportDraft`：优势、改进项、训练步骤，不允许模型自行覆盖确定性总分。
- [ ] 每个结构同时提供 Go Struct、业务校验器和 JSON Schema。
- [ ] 枚举、数量、长度、分数范围、ID 引用、重复项和证据归属都由 Go 校验。

### AGENT-022 统一结构化输出执行器

- [x] Eino StructuredRunner 已执行模型调用、严格 JSON 解码并拒绝未知字段。
- [-] Provider 原生 JSON Schema 与题集领域校验已接通；仍需增加本地通用 JSON Schema Validator。
- [ ] 第一次失败时把精简校验错误交给修复 Prompt，最多修复一次。
- [ ] 第二次仍失败则返回 `AI_OUTPUT_INVALID`，不写半成品业务数据。
- [ ] 保存失败类型和输出哈希，供模型/Prompt 回归分析。

---

## 6. P0-D：真实简历理解

### AGENT-030 将简历抽取升级为异步 AI 节点

- [ ] 保留 Tika 负责文件转文本，LLM 只处理已提取文本。
- [ ] 为文本长度、空白、乱码和超长简历建立预处理规则。
- [ ] 调用 `resume.extract/v1` 生成技能、经历、项目、指标和教育等结构化事实。
- [ ] 每条事实必须包含可验证的原文摘录和所属简历版本。
- [ ] 在同一事务中替换该版本的旧 facts，避免混合多个抽取版本。
- [ ] 保存 `extractor_model`、`prompt_version` 或关联 invocation ID，支持重解析和回溯。
- [ ] 解析失败时保留 Tika 文本和可重试任务状态，不把简历误标为 completed。

### AGENT-031 移除假 Embedding 语义

- [ ] MVP 不再为新 chunk 写入 SHA-256 伪向量。
- [ ] 明确旧 `baseline-hash-1536` 数据的兼容/清理策略。
- [ ] 若暂时保留 chunks，只保存文本切片和 token_count，不宣称具有语义检索能力。
- [ ] Embedding 字段和 pgvector 扩展可作为未来兼容口保留。

验收：

- 使用至少 10 份不同格式简历，事实能定位回原文。
- 模型不得捏造原文不存在的公司、项目、技术或指标。
- 相同简历版本重复解析具备幂等或明确版本语义。

---

## 7. P0-E：真实蓝图与题集生成

### AGENT-040 蓝图数据模型

- [ ] 决定蓝图归属：建议生成后固化到题集，并在创建面试时复制快照。
- [ ] 新增 `blueprint` JSONB 或规范化表，并记录 Schema/Prompt/模型版本。
- [ ] 蓝图至少包含能力键、权重、难度、题数、追问上限和证据范围。
- [ ] 历史面试始终读取创建时快照，不被后续重新生成回写改变。

### AGENT-041 题集生成工作流

- [-] 已输入当前用户的简历原文、可选 JD 和目标岗位；结构化 facts 待真实简历抽取完成后加入。
- [ ] 生成蓝图。
- [-] 已通过 Eino Graph 直接生成题目；蓝图节点尚未建立。
- [-] 已校验数量、连续序号、字段长度和未知字段；重复度、能力覆盖率和证据 ID 待蓝图/事实模型完成。
- [ ] 对不合格部分只做一次定向修复，不重新生成已经合格的全部内容。
- [x] 模型调用已移动到数据库写事务之外，成功校验后才开始写事务。
- [ ] 使用输入资源版本和 Prompt 版本组成幂等键。
- [-] 正式 AI 路径不再调用固定题；关闭 AI 的本地回退仍保留，尚未在资源上标记 degraded。

### AGENT-042 改为异步产品流程

- [ ] 创建题集请求返回 `202 + task_id`，或新增生成任务接口并保持契约清晰。
- [ ] Worker 执行蓝图和题目生成，持续更新进度阶段。
- [ ] 前端展示“读取材料、规划能力、生成题目、质量检查”等真实阶段。
- [ ] 支持失败重试，但必须复用成功节点结果或输入哈希，避免重复计费。
- [ ] 前端不得使用 60 秒长 HTTP 请求等待完整生成。

验收：

- 不同简历/JD 生成结果具有材料相关性和差异性。
- 每道材料相关题都能追溯到事实 ID 或明确标记为岗位通用题。
- 重复、越权证据 ID、非法 JSON 和模型超时都有自动化测试。

---

## 8. P0-F：真实评分与报告

### AGENT-050 确定评分量表

- [ ] 明确五个评分维度、权重及 1/3/5 或 0–100 的锚点定义。
- [ ] 区分“未提供证据”和“能力不足”，避免对简短但有效的回答只按长度扣分。
- [ ] 明确跳过题、空回答和无可用材料时的评分规则。
- [ ] 由 Go 校验各维分数并确定性聚合总分。
- [ ] 报告必须声明评分为训练建议，不作为招聘结论。

### AGENT-051 评分工作流

- [ ] Intent：读取题目意图、能力点和预期回答点。
- [ ] Evidence：从回答、简历事实和上下文中抽取支持/反驳证据。
- [ ] Evaluation：按固定量表输出各维评分和理由。
- [ ] Critique：生成具体、可执行且不捏造经历的改进建议。
- [ ] 对各题评分可受控并行，但限制用户级并发。
- [ ] 单题失败时定义整份报告失败或部分重试策略，禁止静默填充固定高分。

### AGENT-052 报告生成

- [ ] Go 汇总各题维度分数、总分和质量门禁。
- [ ] 报告模型只负责组织优势、改进优先级和训练计划，不自行改分。
- [ ] Golden Answer 必须基于用户已有材料；需要假设的内容明确标为示例表达。
- [ ] evidence 返回事实 ID/回答摘录等稳定引用，而非“用户提供了回答内容”之类泛化文本。
- [ ] 报告与 session 唯一绑定；重复查询复用结果。
- [ ] 删除 `scoreAnswer()` 字数评分和固定 critique/golden answer。

### AGENT-053 报告异步化

- [ ] 完成面试时投递报告任务，GET 报告只读取状态/结果，不在请求事务中调用模型。
- [ ] 支持 `pending/running/completed/failed/degraded` 状态。
- [ ] 前端轮询或订阅状态并提供安全重试入口。
- [ ] 报告生成失败不得影响已经完成的面试和用户回答。

验收：

- 同一回答重复评测的分数波动保持在设定范围。
- 更具体、有证据的回答通常优于空泛或无关回答。
- 报告中的事实、数字和技术必须能追溯到材料或回答。
- 空回答、提示注入、超长回答和中英混合作答均有测试。

---

## 9. P1：动态面试官与收窄 ReAct

真实出题和评分稳定后再实现本阶段，避免首发同时引入过多不确定性。

### AGENT-060 先实现受限决策器

- [ ] 第一版优先采用强类型决策输出，而不是立即开放任意 Tool Calling：

```json
{
  "action": "follow_up",
  "question": "...",
  "capability_key": "...",
  "evidence_fact_ids": ["..."],
  "reason": "..."
}
```

- [ ] `action` 仅允许 `follow_up`、`next_capability`。
- [ ] Go 状态机决定是否接受该动作、是否结束面试以及下一 ordinal。
- [ ] 每轮最多一个追问决策，超时则使用蓝图中的静态下一题。

### AGENT-061 补齐动态会话模型

- [ ] 为 session 保存蓝图快照、当前能力点、已用追问数和模型版本。
- [ ] 为 turn 增加主问题/追问类型、`parent_turn_id`、能力键和生成 invocation ID。
- [ ] 明确动态追问是否占用总题数与总时间。
- [ ] 每轮上下文只包含系统规则、蓝图当前点、最近 2–3 轮和本轮回答。
- [ ] 刷新或 Worker/API 重启后能够从数据库恢复，不依赖进程内消息历史。

### AGENT-062 按需升级为 Tool/ReAct

- [ ] 只有在真实“查简历片段”或“查面经”工具产生价值后才接 ADK ChatModelAgent。
- [ ] 工具必须只读，并在内部强制绑定当前 `user_id`，模型参数不能覆盖租户范围。
- [ ] 工具参数使用 JSON Schema，返回结果限制条数和字符数。
- [ ] 每轮工具调用最多 2 次，循环深度、Token 和时间均有硬上限。
- [ ] 工具超时/空结果时回到蓝图静态下一题。
- [ ] 对工具调用、结果哈希和拒绝原因进行审计。

验收：

- 模型无法自行增加无限轮次或结束整场面试。
- 恶意回答不能诱导工具读取其他用户资料。
- 追问超时、Provider 故障和工具故障均能回退并走完整场面试。

---

## 10. P1：安全、隐私与提示注入

### AGENT-070 Prompt Injection 防护

- [ ] 明确系统指令优先级，简历、JD、面经和回答一律视为不可信数据。
- [ ] 不允许用户内容改变输出 Schema、租户范围、工具列表、预算或系统角色。
- [ ] 工具服务端重新做授权，不信任模型提供的用户或资源 ID。
- [ ] 对“忽略规则”“泄露 Prompt”“读取其他简历”等攻击建立评测集。
- [ ] 日志和错误响应不泄露 System Prompt、API Key 或完整用户材料。

### AGENT-071 数据治理

- [ ] 明确模型供应商的数据保留和训练政策，生产配置选择不用于训练的接口/账户。
- [ ] 在隐私政策中披露简历、JD、回答会被发送给模型供应商处理。
- [ ] 支持账户数据导出、删除以及模型审计记录的合规保留策略。
- [ ] 给模型输入、审计调试正文和对象存储设置保留周期。
- [ ] 对日志中的邮箱、回答正文、简历片段和 Token 做脱敏。

---

## 11. P1：评测、测试与发布门禁

### AGENT-080 Golden Dataset

- [ ] 建立版本化匿名样本，至少覆盖：应届、社招、技术、非技术、中英文、短简历、长简历。
- [ ] 为简历事实抽取标注事实与 source excerpt。
- [ ] 为题集标注相关性、覆盖率、重复度和不可回答题。
- [ ] 为评分标注维度分和理由范围，不追求唯一措辞。
- [ ] 加入空回答、跑题、夸大、Prompt Injection 和敏感内容样本。
- [ ] 每次 Prompt/模型/Schema 升级必须跑回归并保存结果。

### AGENT-081 自动化测试

- [ ] Provider 适配单测：正常、429、5xx、超时、非法 JSON、Token usage 缺失。
- [ ] Prompt 加载与版本不存在测试。
- [ ] 所有领域 Schema 和校验器单测。
- [ ] Fake Model 驱动的简历、出题、评分、报告 workflow 单测。
- [ ] PostgreSQL + Redis + Worker 集成测试：重复投递、崩溃、重试、幂等、死信。
- [ ] 跨用户访问、证据 ID 越权和工具越权安全测试。
- [ ] 注册→上传→解析→JD→题集→面试→报告全流程 E2E。
- [ ] 生产相同配置下的小流量真实 Provider smoke test，密钥只来自 CI Secret。

### AGENT-082 质量门禁

- [ ] 定义事实抽取准确率/召回率基线。
- [ ] 定义题目材料相关率、重复率、能力覆盖率基线。
- [ ] 定义评分稳定性和与人工评分的相关性基线。
- [ ] 定义非法结构输出率、模型错误率、P95 延迟和单次成本上限。
- [ ] 低于任一硬基线时阻止 Prompt 或模型版本上线。
- [ ] 支持快速回滚到上一 Prompt/模型组合。

---

## 12. P1：可观测性与运行保障

### AGENT-090 指标

- [ ] 模型请求量、成功率、重试率、降级率。
- [ ] 首 Token/总耗时；若无流式则至少记录总耗时。
- [ ] 输入、输出、总 Token 和估算费用。
- [ ] 结构化输出首次失败率、修复成功率和最终失败率。
- [ ] 各 workflow 节点耗时和失败率。
- [ ] 题集/报告任务排队时长、执行时长和积压量。
- [ ] 单用户异常调用量及预算拒绝次数。

### AGENT-091 告警与 Runbook

- [ ] Provider 错误率或 P95 延迟超阈值。
- [ ] 非法输出率显著上升。
- [ ] 队列堆积或任务长时间 running。
- [ ] 单用户/全站 Token 或费用异常。
- [ ] 审计写入失败、成本价格表缺失。
- [ ] 为每类告警编写排查、降级、停用和恢复步骤。

---

## 13. P2：未来能力

- [ ] 面经数据源完成合规评估后接入真实检索 Provider。
- [ ] 公共面经达到需要检索的规模后接真实 Embedding。
- [ ] 自研 pgvector Retriever，所有私有查询强制按 `user_id`/未来 workspace 范围过滤。
- [ ] 公共面经库和用户私有材料使用独立数据域与授权策略。
- [ ] 建检索召回率、空结果率、引用正确率评测。
- [ ] 需要外部工具生态时再实现 MCP Tool Adapter。
- [ ] 多次面试后可增加结构化 `user_skill_profile`，但必须允许用户查看、纠正和删除。
- [ ] 有明确产品收益后再考虑流式输出、语音实时面试或更复杂 Agent 协作。

---

## 14. 推荐实施批次

### Batch 1：打通最小真实 AI 路径

- AGENT-001～003：接口、Provider、可靠性。
- AGENT-010：调用审计。
- AGENT-020～022：Prompt 和结构化输出执行器。
- AGENT-030：真实简历事实抽取。

交付物：上传一份简历后，可以得到带原文证据的真实结构化事实。

### Batch 2：做真核心用户价值

- AGENT-040～042：蓝图和题集异步生成。
- AGENT-050～053：评分与报告异步生成。
- 同步接通前端任务状态、题集和报告页面。

交付物：不同材料得到不同题目，完成面试后得到有证据的真实报告。

### Batch 3：建立上线门禁

- AGENT-011：额度与成本。
- AGENT-070～071：安全与数据治理。
- AGENT-080～082：Golden Dataset、测试和门禁。
- AGENT-090～091：监控、告警和 Runbook。

交付物：可观测、可限额、可回滚并通过邀请制 Beta 发布检查。

### Batch 4：增强交互

- AGENT-060～062：动态追问与按需 ReAct。
- P2 面经检索、Embedding、MCP 等未来能力按实际数据规模实施。

交付物：有界、可恢复、失败可降级的动态面试官。

---

## 15. 上线检查清单

### 邀请制 Beta 前必须全部通过

- [ ] 固定五题、字数评分和固定报告已从正式路径移除。
- [ ] Provider 与 Prompt 版本固定，可一键回滚。
- [ ] 题集和报告走异步任务，接口不持长事务等待模型。
- [ ] 所有模型输出通过 Schema 和领域校验。
- [ ] 模型调用审计、Token、成本和 Trace 可查询。
- [ ] 用户并发、日调用和预算硬限制生效。
- [ ] Prompt Injection、跨用户证据访问和日志泄密测试通过。
- [ ] 核心 E2E、Worker 集成测试和 Golden Dataset 达到基线。
- [ ] 模型不可用时题集/报告显示明确失败或降级状态，可安全重试。
- [ ] 隐私政策和 AI 评分声明覆盖第三方模型处理。

### 公开注册前追加

- [ ] 容量与成本压测完成。
- [ ] 监控告警接通并完成故障演练。
- [ ] 数据导出、账户删除和数据保留策略上线。
- [ ] Provider 配额耗尽、区域故障和密钥轮换演练通过。
- [ ] 发布、回滚、数据库备份恢复流程完成演练。

---

## 16. 首轮开发建议

第一轮不要同时实现 Eino Graph、Workflow、ADK、RAG 和 MCP。建议先建立一条最小纵向切片：

```text
真实 Provider
  → model_invocations
  → question.generate/v1
  → Go Struct + Schema 校验
  → Fake Model 单测
  → 一份简历/JD 生成真实题集
```

这条路径验证配置、模型调用、Prompt 加载、结构化输出、审计和业务写库六个关键边界。跑稳后，再复用相同基础扩展简历抽取和评分报告。
