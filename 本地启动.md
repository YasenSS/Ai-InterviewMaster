# 本地启动指南（Linux）

> 本文档基于 2026-07-30 在本机（Ubuntu / bash）实测通过的启动流程整理。
> README 中的命令是 PowerShell 版本，本文是 Linux 等价版本，并补充了国内网络环境下的坑与解法。

## 组件说明

| 组件 | 作用 | 访问地址 |
|---|---|---|
| PostgreSQL + pgvector | 业务数据 + 向量存储 | localhost:5432 |
| Redis | Asynq 任务队列 | localhost:6379 |
| MinIO | S3 兼容对象存储，**保存简历原文件和 ASR 音频** | API: localhost:9000，控制台: http://localhost:9001 |
| Tika | 解析 PDF/DOCX 简历为文本 | localhost:9998 |
| API | Go 后端（go-zero REST） | http://localhost:8080 |
| Worker | Asynq 异步任务（简历解析、ASR 转写、对象清理） | 无端口 |
| Web | Next.js 前端，`/api/v1/*` 自动代理到 8080 | http://localhost:3000 |
| Whisper ASR（可选） | Beta 语音转写服务 | localhost:9002 |

**MinIO 控制台**（http://localhost:9001）是对象存储的 Web 管理界面，可以浏览 `interviewmaster-local` bucket 里上传的简历/音频文件、查看用量。登录账号密码见 `.env` 的 `MINIO_ROOT_USER` / `MINIO_ROOT_PASSWORD`（默认 `interviewmaster` / `interviewmaster_local_only`）。文件流转方式：浏览器向 API 申请预签名 URL → 浏览器直传 MinIO → 通知 API 完成 → Worker 从 MinIO 取文件处理。

## 前置要求

- Go ≥ 1.24、Node ≥ 20、pnpm（`pnpm install` 已在仓库根执行过一次即可）
- Docker 可用。若 `docker ps` 报 `permission denied`：

```bash
sudo usermod -aG docker $USER
newgrp docker   # 或注销重新登录；临时方案：sg docker -c 'docker 命令'
```

## 首次启动（5 步）

### 1. 准备环境变量

```bash
cd ~/Learning/Ai-InterviewMaster   # 注意：必须在项目根目录执行
cp .env.example .env
```

### 2. 启动基础设施容器

```bash
docker compose up -d postgres redis minio tika
docker compose ps   # postgres / redis 应显示 healthy
```

### 3. 执行数据库迁移

```bash
export IM_DATABASE_URL="postgres://interviewmaster:interviewmaster_local_only@localhost:5432/interviewmaster?sslmode=disable"
cd backend && go run github.com/pressly/goose/v3/cmd/goose@v3.27.1 -dir migrations postgres "$IM_DATABASE_URL" up
cd ..
```

### 4. 启动 API、Worker、Web（三个终端）

```bash
# 终端 1：API（:8080）
cd backend && go run -buildvcs=false ./apps/api -f apps/api/etc/interviewmaster.yaml

# 终端 2：Worker
cd backend && go run -buildvcs=false ./apps/worker -f apps/worker/etc/worker.yaml

# 终端 3：Web（:3000）
pnpm --dir web dev
```

`apps/api/etc/interviewmaster.yaml` 的默认连接地址就是 localhost，本机运行无需额外配置。

### 5. 验证

```bash
curl http://localhost:8080/api/v1/health   # {"status":"ok",...}
curl http://localhost:8080/api/v1/ready    # database/redis ok
curl -o /dev/null -w '%{http_code}\n' http://localhost:3000/   # 200
```

打开 http://localhost:3000 注册账号即可使用。

## 可选：启用 Eino 智能层

Eino 默认关闭，因此没有模型账号时仍可运行原有本地闭环。项目当前接入的是 OpenAI Chat Completions 兼容协议，可用于 OpenAI 或提供兼容端点的其他供应商。

在 `.env` 中配置：

```bash
IM_AI_ENABLED=true
IM_AI_PROVIDER=openai-compatible
IM_AI_BASE_URL=https://api.openai.com/v1
IM_AI_API_KEY=替换为真实密钥
IM_AI_CHAT_MODEL=gpt-4.1-mini
IM_AI_REQUEST_TIMEOUT=30s
IM_AI_MAX_OUTPUT_TOKENS=4096
IM_AI_STRUCTURED_OUTPUTS=true
```

启用后 API 会在启动阶段校验 Provider、密钥、模型、超时和 Token 上限，并通过 `ServiceContext.ChatModel` 暴露统一模型接口。业务层不直接依赖 Eino 或供应商 SDK。

当前接通的第一条真实业务路径是题集生成：启用 AI 后，`POST /api/v1/question-sets` 和重新生成接口会读取当前用户的简历原文、可选 JD 与目标岗位，通过 Eino Graph 生成并严格校验 5 道结构化题目；模型调用发生在数据库写事务之外。关闭 AI 时继续使用本地规则题，方便离线开发。

如果兼容供应商不支持 OpenAI 的 `json_schema` 响应格式，将 `IM_AI_STRUCTURED_OUTPUTS=false`；业务侧仍会执行严格 JSON 解码和 Go 领域校验，只是不再向供应商声明原生结构化输出。

不要把真实 API Key 写入 YAML、`.env.example` 或提交到 Git。

## 可选：启动 Beta Whisper ASR

```bash
docker compose --profile beta up -d asr
```

首次启动会自动下载 Whisper base 模型（约 139MB，Azure 直连约 1 分钟，无需代理）。
验证：API 登录后 `POST /api/v1/beta/asr/uploads` 上传音频 → `POST /api/v1/beta/asr/tasks` 投递任务 → `GET /api/v1/tasks/{id}` 查询到 `succeeded` 与转写结果。

## 国内网络故障排查（实测有效）

### 构建镜像时 gcr.io 超时

`backend/Dockerfile` 的运行时基础镜像 `gcr.io/distroless/static-debian12:nonroot` 在国内无法直连。
注意：Docker 的 `registry-mirrors` 配置**只对 Docker Hub 生效，对 gcr.io 无效**。
解法（预拉取 + retag）：

```bash
docker pull gcr.m.daocloud.io/distroless/static-debian12:nonroot
docker tag gcr.m.daocloud.io/distroless/static-debian12:nonroot gcr.io/distroless/static-debian12:nonroot
```

### Docker Hub 拉取大镜像卡在 "Pulling fs layer" 零流量

以 Whisper 镜像（约 3.4GB）为例，直连与多个镜像站（daocloud/nju/sjtu/1ms/rat.dev）均可能停滞。若本机有代理（如 Clash，端口 7897），用 crane 走代理拉取再导入：

```bash
go install github.com/google/go-containerregistry/cmd/crane@latest
HTTPS_PROXY=http://127.0.0.1:7897 ~/go/bin/crane pull onerahmet/openai-whisper-asr-webservice:latest /tmp/asr.tar
docker load -i /tmp/asr.tar && rm /tmp/asr.tar
docker compose --profile beta up -d asr
```

### Go 模块下载慢

```bash
go env -w GOPROXY=https://goproxy.cn,direct
```

## 日常重启 / 停止

```bash
# 停止本机进程：在对应终端 Ctrl+C（API / Worker / Web）
# 停止基础设施（数据保留在 docker volume 中）
docker compose down          # 不含 beta profile 的 asr
docker compose --profile beta down

# 再次启动：基础设施容器会自启（restart 策略外的情况执行第 2 步），
# 然后只需重复第 4 步启动 API / Worker / Web；无需重新迁移。
```

## 备选：全容器模式

```bash
docker compose --profile app up --build -d
```

一条命令起全部服务（含 api / worker / web 容器）。前提：先按上文解决 gcr.io 镜像问题；构建阶段需拉取 `golang` 镜像并执行 `go mod download`，国内网络可能较慢。日常开发推荐本文的混合模式（改代码即时生效，无需重建镜像）。

## 附：最近一次本机验收状态（2026-07-30）

- 迁移：6 组全部应用（00001–00006）
- M0–M3、Beta（面经画像降级、ASR 转写）全部通过运行时实测
- 静态检查：`go vet` / `go test` / `go build`、`eslint` / `tsc` / `vitest` / `next build` 全部通过
